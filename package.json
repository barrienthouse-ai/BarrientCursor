{
  "name": "service-manager-report",
  "version": "1.0.0",
  "private": true,
  "description": "Collision-safe service manager reporting tool for daily tech hours, department gross, heat cases, and repair orders.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
